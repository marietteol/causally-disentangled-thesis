The code comprises of three different parts:
• The SimCLR baseline code
• The SimCLR + adversarial debiasing code
• The SimCLR + adversarial debiasing + causal bottleneck layer code

They are split into three different maps. 
